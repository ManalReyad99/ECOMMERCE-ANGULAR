import { ChangeCreditFormatPipe } from './change-credit-format.pipe';

describe('ChangeCreditFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeCreditFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
